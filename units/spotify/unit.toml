name     = "spotify"
summary  = "Spotify in the launcher: a player, and search that needs no account"
version  = "0.1.0"
category = "Media"
tags     = ["launcher", "music", "mpris"]
author   = "pehcastro"
kind     = "hypr"
needs     = ["busctl", "jq", "curl"]
requires  = ["omacast"]
keys     = []
